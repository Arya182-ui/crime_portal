# Load service account JSON into the env variable (one-liner for PS)
$env:FIREBASE_SERVICE_ACCOUNT = Get-Content 'atm1-21a5f-firebase-adminsdk-2tvpg-419ad9b937.json' -Raw

# Set frontend origin for CORS during local development
$env:FRONTEND_URL = 'http://localhost:3000'

# Run backend (from project root)
mvn -f .\Backend\pom.xml spring-boot:run